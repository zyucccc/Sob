let a = 1 and b = 2 and c = 3 in 
  let discriminant = b*b - 4*a*c in
  if discriminant > 0 then "discriminant positif : deux racines"
  else if discriminant == 0 then "discriminant nul : une seule racine"
  else "discriminant négatif : aucune racine"